<div id="<%= dom_id equipe %>" class="show-card">
  <h3 class="guard-name"><%= equipe.nome %></h3>
  
  <div class="guard-info">
    <div class="info-row">
      <span class="info-label">Unidade:</span>
      <span class="info-value"><%= equipe.unidade ? equipe.unidade.nome : "Não atribuída" %></span>
    </div>
  </div>
  
  <div class="card-actions">
    <%= link_to "Mostrar equipe", equipe, class: "button card-button" %>
  </div>
</div>
